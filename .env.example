# Subfrost API key — get one at https://api.subfrost.io
# Sent as the x-subfrost-api-key header (NOT in the URL path).
SUBFROST_API_KEY=replace-me

# Optional override (defaults to mainnet JSON-RPC gateway):
# SUBFROST_RPC=https://mainnet.subfrost.io/v4/jsonrpc
